const assert = require('chai').assert;
const sinon = require('sinon');

const MapLights = require('../lib/map_lights');

describe('MapLights', function(){
  it.skip('updates the map???', function(){
    // Test when vfr, mvfr, ifr, lifr?
    // Make sure show() is called?
    // Make sure it calls itself no matter what
  })
})
